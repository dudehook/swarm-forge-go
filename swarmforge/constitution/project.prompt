# Project Rules

This is a pipe-flush handoff-structure test.

The configured Codex agents are `one`, `two`, `three`, `four`, `five`, and `six`.

The only intended work is sending and receiving handoffs in this cycle:

one -> two -> three -> four -> five -> six -> one

Do not modify project files, run verification, install tools, or perform implementation work.
